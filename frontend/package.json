{
  "name": "secure-password-generator",
  "version": "1.0.0",
  "scripts": {
    "build": "npm install && npx vite build",
    "dev": "npm install && npx vite"
  }
},
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.1",
    "vite": "^7.3.1"
  }
}
